# 牛顿环曲率半径计算器

这是牛顿环测曲率半径实验的纯计算网页版本。

## 功能

- 输入环级数 `k` 与暗环直径 `d`
- 自动线性拟合并计算透镜曲率半径
- 输出 A 类、B 类、合成标准不确定度和扩展不确定度
- 绘制 `r²-k` 拟合图

## 说明

这个版本只包含前端静态计算页面：

- 不包含 AI 回答助手
- 不包含拍照识别
- 不包含 OCR
- 不需要后端服务器

可以直接部署到 GitHub Pages。
